import ProfileCustomer from "@/components/customer/settings/profile";
import React from "react";

export const metadata = {
  title: "Customer | Profile",
};

export default function ProfilePage() {
  return <ProfileCustomer />;
}
